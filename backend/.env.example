# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=bt4500

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration (for local auth fallback)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Auth0 Configuration (optional - leave empty for local auth only)
# To use Auth0:
# 1. Create a "Regular Web Application" in Auth0 Dashboard
# 2. Enable "Password" grant type in Application > Settings > Advanced > Grant Types
# 3. Create an API in Auth0 Dashboard with identifier as AUTH0_AUDIENCE
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
AUTH0_AUDIENCE=https://your-api-identifier
AUTH0_CONNECTION=Username-Password-Authentication
